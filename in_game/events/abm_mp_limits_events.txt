namespace = abm_mp_limits

# Fired once when the player first transitions into "over alliance limit",
# but only if they've explicitly enabled the alert via the tier list panel.
# Default state for every country is muted (variable not set).
abm_mp_limits.1 = {
	type = country_event

	title = abm_mp_limits.1.title
	desc = abm_mp_limits.1.desc
	outcome = negative

	trigger = {
		is_human = yes
	}

	option = {
		name = abm_mp_limits.1.a
	}
}

# Fired once when the player first transitions into "over fort limit",
# but only if they've explicitly enabled the alert via the tier list panel.
# Default state for every country is muted (variable not set).
abm_mp_limits.2 = {
	type = country_event

	title = abm_mp_limits.2.title
	desc = abm_mp_limits.2.desc
	outcome = negative

	trigger = {
		is_human = yes
	}

	option = {
		name = abm_mp_limits.2.a
	}
}
