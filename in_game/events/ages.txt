namespace = ages_of_eu
namespace = abm_focus_picks

# Ars Belli: same-name replacement of vanilla in_game/events/ages.txt (listed in replaced_files.txt).
# GENERATED by .tools/scripts/focus_picks/generate_focus_picks.js - edit focus_data.js and re-run.
#
# Vanilla ages_of_eu.1 (fired by the hardcoded on_new_age) offered one adm/dip/mil focus via
# set_age_preference. That engine slot allows a single pick, so it is dropped entirely.
# Instead every age from the Renaissance on asks for two picks, each out of 3 branches:
#   ages_of_eu.1        - Economic pick (economy and diplomacy branches)
#   abm_focus_picks.1   - Military pick (two land branches + one naval branch)
# A pick sets the country variable abm_focus_<branch>; the advances of that branch gate on it
# (see in_game/common/advances/abm_focus_picks_*.txt). Variables are never cleared, so picks
# from earlier ages stay researchable.
#
# Both are country_event, NOT age_event: the age event window (eventwindow.gui AgeEventWindow)
# has exactly three cards bound via EventWindow.AccessOptionItemWithKey('adm'/'dip'/'mil') with
# hardcoded Administrative/Diplomatic/Military tooltips. Options without those keys all fall back
# to the first option (every card showed the same focus in game), so it can't list our branches.

ages_of_eu.1 = {
	type = country_event

	title = ages_of_eu.1.title
	desc = abm_focus_picks.economic.desc
	outcome = neutral

	illustration_tags = {
		10 = ages
	}

	# Age of Traditions has no focus picks.
	trigger = {
		NOT = { current_age = age_1_traditions }
	}

	option = {
		name = abm_focus_pick_court
		trigger = { current_age = age_2_renaissance }
		hidden_effect = { set_variable = abm_focus_court }
		custom_tooltip = abm_focus_pick_court_effect_tt
		trigger_event_non_silently = { id = abm_focus_picks.1 }

		ai_will_select = {
			value = 100
		}
	}
	option = {
		name = abm_focus_pick_trade
		trigger = { current_age = age_2_renaissance }
		hidden_effect = { set_variable = abm_focus_trade }
		custom_tooltip = abm_focus_pick_trade_effect_tt
		trigger_event_non_silently = { id = abm_focus_picks.1 }

		ai_will_select = {
			value = 100
		}
	}
	option = {
		name = abm_focus_pick_influence
		trigger = { current_age = age_2_renaissance }
		hidden_effect = { set_variable = abm_focus_influence }
		custom_tooltip = abm_focus_pick_influence_effect_tt
		trigger_event_non_silently = { id = abm_focus_picks.1 }

		ai_will_select = {
			value = 100
		}
	}
	option = {
		name = abm_focus_pick_infrastructure
		trigger = { current_age = age_3_discovery }
		hidden_effect = { set_variable = abm_focus_infrastructure }
		custom_tooltip = abm_focus_pick_infrastructure_effect_tt
		trigger_event_non_silently = { id = abm_focus_picks.1 }

		ai_will_select = {
			value = 100
		}
	}
	option = {
		name = abm_focus_pick_exploration
		trigger = { current_age = age_3_discovery }
		hidden_effect = { set_variable = abm_focus_exploration }
		custom_tooltip = abm_focus_pick_exploration_effect_tt
		trigger_event_non_silently = { id = abm_focus_picks.1 }

		ai_will_select = {
			value = 100
		}
	}
	option = {
		name = abm_focus_pick_diplomatic
		trigger = { current_age = age_3_discovery }
		hidden_effect = { set_variable = abm_focus_diplomatic }
		custom_tooltip = abm_focus_pick_diplomatic_effect_tt
		trigger_event_non_silently = { id = abm_focus_picks.1 }

		ai_will_select = {
			value = 100
		}
	}
	option = {
		name = abm_focus_pick_religion
		trigger = { current_age = age_4_reformation }
		hidden_effect = { set_variable = abm_focus_religion }
		custom_tooltip = abm_focus_pick_religion_effect_tt
		trigger_event_non_silently = { id = abm_focus_picks.1 }

		ai_will_select = {
			value = 100
		}
	}
	option = {
		name = abm_focus_pick_expansion
		trigger = { current_age = age_4_reformation }
		hidden_effect = { set_variable = abm_focus_expansion }
		custom_tooltip = abm_focus_pick_expansion_effect_tt
		trigger_event_non_silently = { id = abm_focus_picks.1 }

		ai_will_select = {
			value = 100
		}
	}
	option = {
		name = abm_focus_pick_humanism
		trigger = { current_age = age_4_reformation }
		hidden_effect = { set_variable = abm_focus_humanism }
		custom_tooltip = abm_focus_pick_humanism_effect_tt
		trigger_event_non_silently = { id = abm_focus_picks.1 }

		ai_will_select = {
			value = 100
		}
	}
	option = {
		name = abm_focus_pick_administration
		trigger = { current_age = age_5_absolutism }
		hidden_effect = { set_variable = abm_focus_administration }
		custom_tooltip = abm_focus_pick_administration_effect_tt
		trigger_event_non_silently = { id = abm_focus_picks.1 }

		ai_will_select = {
			value = 100
		}
	}
	option = {
		name = abm_focus_pick_commerce
		trigger = { current_age = age_5_absolutism }
		hidden_effect = { set_variable = abm_focus_commerce }
		custom_tooltip = abm_focus_pick_commerce_effect_tt
		trigger_event_non_silently = { id = abm_focus_picks.1 }

		ai_will_select = {
			value = 100
		}
	}
	option = {
		name = abm_focus_pick_espionage
		trigger = { current_age = age_5_absolutism }
		hidden_effect = { set_variable = abm_focus_espionage }
		custom_tooltip = abm_focus_pick_espionage_effect_tt
		trigger_event_non_silently = { id = abm_focus_picks.1 }

		ai_will_select = {
			value = 100
		}
	}
	option = {
		name = abm_focus_pick_industry
		trigger = { current_age = age_6_revolutions }
		hidden_effect = { set_variable = abm_focus_industry }
		custom_tooltip = abm_focus_pick_industry_effect_tt
		trigger_event_non_silently = { id = abm_focus_picks.1 }

		ai_will_select = {
			value = 100
		}
	}
	option = {
		name = abm_focus_pick_empire
		trigger = { current_age = age_6_revolutions }
		hidden_effect = { set_variable = abm_focus_empire }
		custom_tooltip = abm_focus_pick_empire_effect_tt
		trigger_event_non_silently = { id = abm_focus_picks.1 }

		ai_will_select = {
			value = 100
		}
	}
	option = {
		name = abm_focus_pick_innovation
		trigger = { current_age = age_6_revolutions }
		hidden_effect = { set_variable = abm_focus_innovation }
		custom_tooltip = abm_focus_pick_innovation_effect_tt
		trigger_event_non_silently = { id = abm_focus_picks.1 }

		ai_will_select = {
			value = 100
		}
	}
}

abm_focus_picks.1 = {
	type = country_event

	title = abm_focus_picks.1.title
	desc = abm_focus_picks.1.desc
	outcome = neutral

	illustration_tags = {
		10 = ages
	}

	trigger = {
		NOT = { current_age = age_1_traditions }
	}

	option = {
		name = abm_focus_pick_levy
		trigger = { current_age = age_2_renaissance }
		hidden_effect = { set_variable = abm_focus_levy }
		custom_tooltip = abm_focus_pick_levy_effect_tt

		ai_will_select = {
			value = 100
		}
	}
	option = {
		name = abm_focus_pick_mercenary
		trigger = { current_age = age_2_renaissance }
		hidden_effect = { set_variable = abm_focus_mercenary }
		custom_tooltip = abm_focus_pick_mercenary_effect_tt

		ai_will_select = {
			value = 100
		}
	}
	option = {
		name = abm_focus_pick_maritime
		trigger = { current_age = age_2_renaissance }
		hidden_effect = { set_variable = abm_focus_maritime }
		custom_tooltip = abm_focus_pick_maritime_effect_tt

		ai_will_select = {
			value = 50
		}
	}
	option = {
		name = abm_focus_pick_quality
		trigger = { current_age = age_3_discovery }
		hidden_effect = { set_variable = abm_focus_quality }
		custom_tooltip = abm_focus_pick_quality_effect_tt

		ai_will_select = {
			value = 100
		}
	}
	option = {
		name = abm_focus_pick_quantity
		trigger = { current_age = age_3_discovery }
		hidden_effect = { set_variable = abm_focus_quantity }
		custom_tooltip = abm_focus_pick_quantity_effect_tt

		ai_will_select = {
			value = 100
		}
	}
	option = {
		name = abm_focus_pick_naval
		trigger = { current_age = age_3_discovery }
		hidden_effect = { set_variable = abm_focus_naval }
		custom_tooltip = abm_focus_pick_naval_effect_tt

		ai_will_select = {
			value = 50
		}
	}
	option = {
		name = abm_focus_pick_offense
		trigger = { current_age = age_4_reformation }
		hidden_effect = { set_variable = abm_focus_offense }
		custom_tooltip = abm_focus_pick_offense_effect_tt

		ai_will_select = {
			value = 100
		}
	}
	option = {
		name = abm_focus_pick_defense
		trigger = { current_age = age_4_reformation }
		hidden_effect = { set_variable = abm_focus_defense }
		custom_tooltip = abm_focus_pick_defense_effect_tt

		ai_will_select = {
			value = 100
		}
	}
	option = {
		name = abm_focus_pick_littoral_fleet
		trigger = { current_age = age_4_reformation }
		hidden_effect = { set_variable = abm_focus_littoral_fleet }
		custom_tooltip = abm_focus_pick_littoral_fleet_effect_tt

		ai_will_select = {
			value = 50
		}
	}
	option = {
		name = abm_focus_pick_maneuver
		trigger = { current_age = age_5_absolutism }
		hidden_effect = { set_variable = abm_focus_maneuver }
		custom_tooltip = abm_focus_pick_maneuver_effect_tt

		ai_will_select = {
			value = 100
		}
	}
	option = {
		name = abm_focus_pick_logistics
		trigger = { current_age = age_5_absolutism }
		hidden_effect = { set_variable = abm_focus_logistics }
		custom_tooltip = abm_focus_pick_logistics_effect_tt

		ai_will_select = {
			value = 100
		}
	}
	option = {
		name = abm_focus_pick_highseas_fleet
		trigger = { current_age = age_5_absolutism }
		hidden_effect = { set_variable = abm_focus_highseas_fleet }
		custom_tooltip = abm_focus_pick_highseas_fleet_effect_tt

		ai_will_select = {
			value = 50
		}
	}
	option = {
		name = abm_focus_pick_voluntary_service
		trigger = { current_age = age_6_revolutions }
		hidden_effect = { set_variable = abm_focus_voluntary_service }
		custom_tooltip = abm_focus_pick_voluntary_service_effect_tt

		ai_will_select = {
			value = 100
		}
	}
	option = {
		name = abm_focus_pick_conscription
		trigger = { current_age = age_6_revolutions }
		hidden_effect = { set_variable = abm_focus_conscription }
		custom_tooltip = abm_focus_pick_conscription_effect_tt

		ai_will_select = {
			value = 100
		}
	}
	option = {
		name = abm_focus_pick_insurrection
		trigger = { current_age = age_6_revolutions }
		hidden_effect = { set_variable = abm_focus_insurrection }
		custom_tooltip = abm_focus_pick_insurrection_effect_tt

		ai_will_select = {
			value = 100
		}
	}
}
