# Evaluated in the scope of the country that HOLDS the antagonism (scope:actor).
# The paired bias has value = -1, so these return the amount forgiven.
# min = 0 guards against a negative pool flipping the sign and ADDING antagonism.

ars_belli_antagonism_forgive_half = {
	value = "antagonism(scope:recipient)"
	multiply = 0.5
	min = 0
}

# Flat 200, but never more than is actually there, so we do not push the pool negative.
ars_belli_antagonism_forgive_200 = {
	value = "antagonism(scope:recipient)"
	min = 0
	max = 200
}
