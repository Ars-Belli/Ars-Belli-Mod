# Extends the hardcoded on_new_age so that, in addition to vanilla
# ages_of_eu.1 (adm/dip/mil), every country also receives a second
# age-start event with its own 3 options.
on_new_age = {
	events = {
		ars_belli_age_focus.1
	}
}
