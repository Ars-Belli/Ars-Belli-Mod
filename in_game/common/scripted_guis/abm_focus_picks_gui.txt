# Ars Belli focus picks: scripted GUIs for the Focus Picks panel. GENERATED by
# .tools/scripts/focus_picks/generate_focus_picks.js - edit focus_data.js and re-run.
# Change points: abm_focus_changes_used counts switches (missing = 0); at most 3.

abm_focus_pick_court = {
	scope = country
	is_shown = { NOT = { OR = { has_variable = abm_focus_court has_variable = abm_focus_trade has_variable = abm_focus_influence } } }
	is_valid = {
		custom_tooltip = {
			text = abm_focus_panel.age_2_renaissance.reached_tt
			OR = { current_age = age_2_renaissance current_age = age_3_discovery current_age = age_4_reformation current_age = age_5_absolutism current_age = age_6_revolutions }
		}
	}
	effect = { set_variable = abm_focus_court }
}

abm_focus_switch_court = {
	scope = country
	is_shown = {
		NOT = { has_variable = abm_focus_court }
		OR = { has_variable = abm_focus_trade has_variable = abm_focus_influence }
	}
	is_valid = {
		custom_tooltip = {
			text = ABM_FOCUS_CHANGES_NONE_TT
			OR = {
				NOT = { has_variable = abm_focus_changes_used }
				var:abm_focus_changes_used < 3
			}
		}
		custom_tooltip = {
			text = ABM_FOCUS_NOTHING_RESEARCHED_TT
			NOT = { OR = { has_advance = shrewd_commerce_practice has_advance = merchant_traditions has_advance = additional_merchants has_advance = merchant_adventures has_advance = debt_and_loans has_advance = abm_protected_trade_caravans has_advance = integrated_elites has_advance = tribute_system has_advance = marcher_lords has_advance = buffer_states has_advance = benign_diplomats has_advance = additional_loyalist_recruitment } }
		}
	}
	effect = {
		if = { limit = { has_variable = abm_focus_trade } remove_variable = abm_focus_trade }
		if = { limit = { has_variable = abm_focus_influence } remove_variable = abm_focus_influence }
		set_variable = abm_focus_court
		if = {
			limit = { has_variable = abm_focus_changes_used }
			change_variable = { name = abm_focus_changes_used add = 1 }
		}
		else = { set_variable = { name = abm_focus_changes_used value = 1 } }
	}
}

abm_focus_pick_trade = {
	scope = country
	is_shown = { NOT = { OR = { has_variable = abm_focus_court has_variable = abm_focus_trade has_variable = abm_focus_influence } } }
	is_valid = {
		custom_tooltip = {
			text = abm_focus_panel.age_2_renaissance.reached_tt
			OR = { current_age = age_2_renaissance current_age = age_3_discovery current_age = age_4_reformation current_age = age_5_absolutism current_age = age_6_revolutions }
		}
	}
	effect = { set_variable = abm_focus_trade }
}

abm_focus_switch_trade = {
	scope = country
	is_shown = {
		NOT = { has_variable = abm_focus_trade }
		OR = { has_variable = abm_focus_court has_variable = abm_focus_influence }
	}
	is_valid = {
		custom_tooltip = {
			text = ABM_FOCUS_CHANGES_NONE_TT
			OR = {
				NOT = { has_variable = abm_focus_changes_used }
				var:abm_focus_changes_used < 3
			}
		}
		custom_tooltip = {
			text = ABM_FOCUS_NOTHING_RESEARCHED_TT
			NOT = { OR = { has_advance = innovativeness has_advance = state_efficiency has_advance = court_accounting has_advance = dynamic_court has_advance = church_attendance_duty has_advance = empiricism has_advance = integrated_elites has_advance = tribute_system has_advance = marcher_lords has_advance = buffer_states has_advance = benign_diplomats has_advance = additional_loyalist_recruitment } }
		}
	}
	effect = {
		if = { limit = { has_variable = abm_focus_court } remove_variable = abm_focus_court }
		if = { limit = { has_variable = abm_focus_influence } remove_variable = abm_focus_influence }
		set_variable = abm_focus_trade
		if = {
			limit = { has_variable = abm_focus_changes_used }
			change_variable = { name = abm_focus_changes_used add = 1 }
		}
		else = { set_variable = { name = abm_focus_changes_used value = 1 } }
	}
}

abm_focus_pick_influence = {
	scope = country
	is_shown = { NOT = { OR = { has_variable = abm_focus_court has_variable = abm_focus_trade has_variable = abm_focus_influence } } }
	is_valid = {
		custom_tooltip = {
			text = abm_focus_panel.age_2_renaissance.reached_tt
			OR = { current_age = age_2_renaissance current_age = age_3_discovery current_age = age_4_reformation current_age = age_5_absolutism current_age = age_6_revolutions }
		}
	}
	effect = { set_variable = abm_focus_influence }
}

abm_focus_switch_influence = {
	scope = country
	is_shown = {
		NOT = { has_variable = abm_focus_influence }
		OR = { has_variable = abm_focus_court has_variable = abm_focus_trade }
	}
	is_valid = {
		custom_tooltip = {
			text = ABM_FOCUS_CHANGES_NONE_TT
			OR = {
				NOT = { has_variable = abm_focus_changes_used }
				var:abm_focus_changes_used < 3
			}
		}
		custom_tooltip = {
			text = ABM_FOCUS_NOTHING_RESEARCHED_TT
			NOT = { OR = { has_advance = innovativeness has_advance = state_efficiency has_advance = court_accounting has_advance = dynamic_court has_advance = church_attendance_duty has_advance = empiricism has_advance = shrewd_commerce_practice has_advance = merchant_traditions has_advance = additional_merchants has_advance = merchant_adventures has_advance = debt_and_loans has_advance = abm_protected_trade_caravans } }
		}
	}
	effect = {
		if = { limit = { has_variable = abm_focus_court } remove_variable = abm_focus_court }
		if = { limit = { has_variable = abm_focus_trade } remove_variable = abm_focus_trade }
		set_variable = abm_focus_influence
		if = {
			limit = { has_variable = abm_focus_changes_used }
			change_variable = { name = abm_focus_changes_used add = 1 }
		}
		else = { set_variable = { name = abm_focus_changes_used value = 1 } }
	}
}

abm_focus_pick_levy = {
	scope = country
	is_shown = { NOT = { OR = { has_variable = abm_focus_levy has_variable = abm_focus_mercenary has_variable = abm_focus_maritime } } }
	is_valid = {
		custom_tooltip = {
			text = abm_focus_panel.age_2_renaissance.reached_tt
			OR = { current_age = age_2_renaissance current_age = age_3_discovery current_age = age_4_reformation current_age = age_5_absolutism current_age = age_6_revolutions }
		}
	}
	effect = { set_variable = abm_focus_levy }
}

abm_focus_switch_levy = {
	scope = country
	is_shown = {
		NOT = { has_variable = abm_focus_levy }
		OR = { has_variable = abm_focus_mercenary has_variable = abm_focus_maritime }
	}
	is_valid = {
		custom_tooltip = {
			text = ABM_FOCUS_CHANGES_NONE_TT
			OR = {
				NOT = { has_variable = abm_focus_changes_used }
				var:abm_focus_changes_used < 3
			}
		}
		custom_tooltip = {
			text = ABM_FOCUS_NOTHING_RESEARCHED_TT
			NOT = { OR = { has_advance = benefits_for_mercenaries has_advance = organised_mercenary_recruitment has_advance = organised_mercenary_payment has_advance = glorious_arms has_advance = abm_ensure_mercenary_payments has_advance = tolerance_idea has_advance = improved_rams has_advance = boarding_parties has_advance = marine_regiments has_advance = naval_glory has_advance = privateers has_advance = ships_penny } }
		}
	}
	effect = {
		if = { limit = { has_variable = abm_focus_mercenary } remove_variable = abm_focus_mercenary }
		if = { limit = { has_variable = abm_focus_maritime } remove_variable = abm_focus_maritime }
		set_variable = abm_focus_levy
		if = {
			limit = { has_variable = abm_focus_changes_used }
			change_variable = { name = abm_focus_changes_used add = 1 }
		}
		else = { set_variable = { name = abm_focus_changes_used value = 1 } }
	}
}

abm_focus_pick_mercenary = {
	scope = country
	is_shown = { NOT = { OR = { has_variable = abm_focus_levy has_variable = abm_focus_mercenary has_variable = abm_focus_maritime } } }
	is_valid = {
		custom_tooltip = {
			text = abm_focus_panel.age_2_renaissance.reached_tt
			OR = { current_age = age_2_renaissance current_age = age_3_discovery current_age = age_4_reformation current_age = age_5_absolutism current_age = age_6_revolutions }
		}
	}
	effect = { set_variable = abm_focus_mercenary }
}

abm_focus_switch_mercenary = {
	scope = country
	is_shown = {
		NOT = { has_variable = abm_focus_mercenary }
		OR = { has_variable = abm_focus_levy has_variable = abm_focus_maritime }
	}
	is_valid = {
		custom_tooltip = {
			text = ABM_FOCUS_CHANGES_NONE_TT
			OR = {
				NOT = { has_variable = abm_focus_changes_used }
				var:abm_focus_changes_used < 3
			}
		}
		custom_tooltip = {
			text = ABM_FOCUS_NOTHING_RESEARCHED_TT
			NOT = { OR = { has_advance = enforced_service has_advance = open_levy_standards has_advance = regular_levy_training has_advance = battlefield_commisions has_advance = abm_local_militias has_advance = abm_feudal_obligations has_advance = improved_rams has_advance = boarding_parties has_advance = marine_regiments has_advance = naval_glory has_advance = privateers has_advance = ships_penny } }
		}
	}
	effect = {
		if = { limit = { has_variable = abm_focus_levy } remove_variable = abm_focus_levy }
		if = { limit = { has_variable = abm_focus_maritime } remove_variable = abm_focus_maritime }
		set_variable = abm_focus_mercenary
		if = {
			limit = { has_variable = abm_focus_changes_used }
			change_variable = { name = abm_focus_changes_used add = 1 }
		}
		else = { set_variable = { name = abm_focus_changes_used value = 1 } }
	}
}

abm_focus_pick_maritime = {
	scope = country
	is_shown = { NOT = { OR = { has_variable = abm_focus_levy has_variable = abm_focus_mercenary has_variable = abm_focus_maritime } } }
	is_valid = {
		custom_tooltip = {
			text = abm_focus_panel.age_2_renaissance.reached_tt
			OR = { current_age = age_2_renaissance current_age = age_3_discovery current_age = age_4_reformation current_age = age_5_absolutism current_age = age_6_revolutions }
		}
	}
	effect = { set_variable = abm_focus_maritime }
}

abm_focus_switch_maritime = {
	scope = country
	is_shown = {
		NOT = { has_variable = abm_focus_maritime }
		OR = { has_variable = abm_focus_levy has_variable = abm_focus_mercenary }
	}
	is_valid = {
		custom_tooltip = {
			text = ABM_FOCUS_CHANGES_NONE_TT
			OR = {
				NOT = { has_variable = abm_focus_changes_used }
				var:abm_focus_changes_used < 3
			}
		}
		custom_tooltip = {
			text = ABM_FOCUS_NOTHING_RESEARCHED_TT
			NOT = { OR = { has_advance = enforced_service has_advance = open_levy_standards has_advance = regular_levy_training has_advance = battlefield_commisions has_advance = abm_local_militias has_advance = abm_feudal_obligations has_advance = benefits_for_mercenaries has_advance = organised_mercenary_recruitment has_advance = organised_mercenary_payment has_advance = glorious_arms has_advance = abm_ensure_mercenary_payments has_advance = tolerance_idea } }
		}
	}
	effect = {
		if = { limit = { has_variable = abm_focus_levy } remove_variable = abm_focus_levy }
		if = { limit = { has_variable = abm_focus_mercenary } remove_variable = abm_focus_mercenary }
		set_variable = abm_focus_maritime
		if = {
			limit = { has_variable = abm_focus_changes_used }
			change_variable = { name = abm_focus_changes_used add = 1 }
		}
		else = { set_variable = { name = abm_focus_changes_used value = 1 } }
	}
}

abm_focus_pick_infrastructure = {
	scope = country
	is_shown = { NOT = { OR = { has_variable = abm_focus_infrastructure has_variable = abm_focus_exploration has_variable = abm_focus_diplomatic } } }
	is_valid = {
		custom_tooltip = {
			text = abm_focus_panel.age_3_discovery.reached_tt
			OR = { current_age = age_3_discovery current_age = age_4_reformation current_age = age_5_absolutism current_age = age_6_revolutions }
		}
	}
	effect = { set_variable = abm_focus_infrastructure }
}

abm_focus_switch_infrastructure = {
	scope = country
	is_shown = {
		NOT = { has_variable = abm_focus_infrastructure }
		OR = { has_variable = abm_focus_exploration has_variable = abm_focus_diplomatic }
	}
	is_valid = {
		custom_tooltip = {
			text = ABM_FOCUS_CHANGES_NONE_TT
			OR = {
				NOT = { has_variable = abm_focus_changes_used }
				var:abm_focus_changes_used < 3
			}
		}
		custom_tooltip = {
			text = ABM_FOCUS_NOTHING_RESEARCHED_TT
			NOT = { OR = { has_advance = quest_for_the_new_world has_advance = exploration has_advance = overseas_exploration has_advance = faster_colonists has_advance = colonial_ventures has_advance = overseas_trade has_advance = flexible_negotiation has_advance = formal_diplomacy has_advance = diplomatic_influence has_advance = adaptability has_advance = diplomatic_corps has_advance = indirect_rule } }
		}
	}
	effect = {
		if = { limit = { has_variable = abm_focus_exploration } remove_variable = abm_focus_exploration }
		if = { limit = { has_variable = abm_focus_diplomatic } remove_variable = abm_focus_diplomatic }
		set_variable = abm_focus_infrastructure
		if = {
			limit = { has_variable = abm_focus_changes_used }
			change_variable = { name = abm_focus_changes_used add = 1 }
		}
		else = { set_variable = { name = abm_focus_changes_used value = 1 } }
	}
}

abm_focus_pick_exploration = {
	scope = country
	is_shown = { NOT = { OR = { has_variable = abm_focus_infrastructure has_variable = abm_focus_exploration has_variable = abm_focus_diplomatic } } }
	is_valid = {
		custom_tooltip = {
			text = abm_focus_panel.age_3_discovery.reached_tt
			OR = { current_age = age_3_discovery current_age = age_4_reformation current_age = age_5_absolutism current_age = age_6_revolutions }
		}
	}
	effect = { set_variable = abm_focus_exploration }
}

abm_focus_switch_exploration = {
	scope = country
	is_shown = {
		NOT = { has_variable = abm_focus_exploration }
		OR = { has_variable = abm_focus_infrastructure has_variable = abm_focus_diplomatic }
	}
	is_valid = {
		custom_tooltip = {
			text = ABM_FOCUS_CHANGES_NONE_TT
			OR = {
				NOT = { has_variable = abm_focus_changes_used }
				var:abm_focus_changes_used < 3
			}
		}
		custom_tooltip = {
			text = ABM_FOCUS_NOTHING_RESEARCHED_TT
			NOT = { OR = { has_advance = organised_construction has_advance = efficient_construction_plans has_advance = expanded_infrastructure_network has_advance = efficient_mining has_advance = rgo_logistics_mil_choice has_advance = advanced_bureaucracy has_advance = flexible_negotiation has_advance = formal_diplomacy has_advance = diplomatic_influence has_advance = adaptability has_advance = diplomatic_corps has_advance = indirect_rule } }
		}
	}
	effect = {
		if = { limit = { has_variable = abm_focus_infrastructure } remove_variable = abm_focus_infrastructure }
		if = { limit = { has_variable = abm_focus_diplomatic } remove_variable = abm_focus_diplomatic }
		set_variable = abm_focus_exploration
		if = {
			limit = { has_variable = abm_focus_changes_used }
			change_variable = { name = abm_focus_changes_used add = 1 }
		}
		else = { set_variable = { name = abm_focus_changes_used value = 1 } }
	}
}

abm_focus_pick_diplomatic = {
	scope = country
	is_shown = { NOT = { OR = { has_variable = abm_focus_infrastructure has_variable = abm_focus_exploration has_variable = abm_focus_diplomatic } } }
	is_valid = {
		custom_tooltip = {
			text = abm_focus_panel.age_3_discovery.reached_tt
			OR = { current_age = age_3_discovery current_age = age_4_reformation current_age = age_5_absolutism current_age = age_6_revolutions }
		}
	}
	effect = { set_variable = abm_focus_diplomatic }
}

abm_focus_switch_diplomatic = {
	scope = country
	is_shown = {
		NOT = { has_variable = abm_focus_diplomatic }
		OR = { has_variable = abm_focus_infrastructure has_variable = abm_focus_exploration }
	}
	is_valid = {
		custom_tooltip = {
			text = ABM_FOCUS_CHANGES_NONE_TT
			OR = {
				NOT = { has_variable = abm_focus_changes_used }
				var:abm_focus_changes_used < 3
			}
		}
		custom_tooltip = {
			text = ABM_FOCUS_NOTHING_RESEARCHED_TT
			NOT = { OR = { has_advance = organised_construction has_advance = efficient_construction_plans has_advance = expanded_infrastructure_network has_advance = efficient_mining has_advance = rgo_logistics_mil_choice has_advance = advanced_bureaucracy has_advance = quest_for_the_new_world has_advance = exploration has_advance = overseas_exploration has_advance = faster_colonists has_advance = colonial_ventures has_advance = overseas_trade } }
		}
	}
	effect = {
		if = { limit = { has_variable = abm_focus_infrastructure } remove_variable = abm_focus_infrastructure }
		if = { limit = { has_variable = abm_focus_exploration } remove_variable = abm_focus_exploration }
		set_variable = abm_focus_diplomatic
		if = {
			limit = { has_variable = abm_focus_changes_used }
			change_variable = { name = abm_focus_changes_used add = 1 }
		}
		else = { set_variable = { name = abm_focus_changes_used value = 1 } }
	}
}

abm_focus_pick_quality = {
	scope = country
	is_shown = { NOT = { OR = { has_variable = abm_focus_quality has_variable = abm_focus_quantity has_variable = abm_focus_naval } } }
	is_valid = {
		custom_tooltip = {
			text = abm_focus_panel.age_3_discovery.reached_tt
			OR = { current_age = age_3_discovery current_age = age_4_reformation current_age = age_5_absolutism current_age = age_6_revolutions }
		}
	}
	effect = { set_variable = abm_focus_quality }
}

abm_focus_switch_quality = {
	scope = country
	is_shown = {
		NOT = { has_variable = abm_focus_quality }
		OR = { has_variable = abm_focus_quantity has_variable = abm_focus_naval }
	}
	is_valid = {
		custom_tooltip = {
			text = ABM_FOCUS_CHANGES_NONE_TT
			OR = {
				NOT = { has_variable = abm_focus_changes_used }
				var:abm_focus_changes_used < 3
			}
		}
		custom_tooltip = {
			text = ABM_FOCUS_NOTHING_RESEARCHED_TT
			NOT = { OR = { has_advance = abm_rapid_reinforcement has_advance = camp_followers has_advance = the_old_and_infirm has_advance = abm_deep_reserves has_advance = supply_trains has_advance = grand_army has_advance = oak_forests_for_ships has_advance = abm_patrol_vessels has_advance = abm_naval_education has_advance = copper_bottoms has_advance = naval_drill has_advance = grand_navy } }
		}
	}
	effect = {
		if = { limit = { has_variable = abm_focus_quantity } remove_variable = abm_focus_quantity }
		if = { limit = { has_variable = abm_focus_naval } remove_variable = abm_focus_naval }
		set_variable = abm_focus_quality
		if = {
			limit = { has_variable = abm_focus_changes_used }
			change_variable = { name = abm_focus_changes_used add = 1 }
		}
		else = { set_variable = { name = abm_focus_changes_used value = 1 } }
	}
}

abm_focus_pick_quantity = {
	scope = country
	is_shown = { NOT = { OR = { has_variable = abm_focus_quality has_variable = abm_focus_quantity has_variable = abm_focus_naval } } }
	is_valid = {
		custom_tooltip = {
			text = abm_focus_panel.age_3_discovery.reached_tt
			OR = { current_age = age_3_discovery current_age = age_4_reformation current_age = age_5_absolutism current_age = age_6_revolutions }
		}
	}
	effect = { set_variable = abm_focus_quantity }
}

abm_focus_switch_quantity = {
	scope = country
	is_shown = {
		NOT = { has_variable = abm_focus_quantity }
		OR = { has_variable = abm_focus_quality has_variable = abm_focus_naval }
	}
	is_valid = {
		custom_tooltip = {
			text = ABM_FOCUS_CHANGES_NONE_TT
			OR = {
				NOT = { has_variable = abm_focus_changes_used }
				var:abm_focus_changes_used < 3
			}
		}
		custom_tooltip = {
			text = ABM_FOCUS_NOTHING_RESEARCHED_TT
			NOT = { OR = { has_advance = formalized_officer_corps has_advance = administrative_leadership has_advance = quality_education has_advance = abm_drill_sergeants has_advance = abm_trained_march has_advance = military_drill has_advance = oak_forests_for_ships has_advance = abm_patrol_vessels has_advance = abm_naval_education has_advance = copper_bottoms has_advance = naval_drill has_advance = grand_navy } }
		}
	}
	effect = {
		if = { limit = { has_variable = abm_focus_quality } remove_variable = abm_focus_quality }
		if = { limit = { has_variable = abm_focus_naval } remove_variable = abm_focus_naval }
		set_variable = abm_focus_quantity
		if = {
			limit = { has_variable = abm_focus_changes_used }
			change_variable = { name = abm_focus_changes_used add = 1 }
		}
		else = { set_variable = { name = abm_focus_changes_used value = 1 } }
	}
}

abm_focus_pick_naval = {
	scope = country
	is_shown = { NOT = { OR = { has_variable = abm_focus_quality has_variable = abm_focus_quantity has_variable = abm_focus_naval } } }
	is_valid = {
		custom_tooltip = {
			text = abm_focus_panel.age_3_discovery.reached_tt
			OR = { current_age = age_3_discovery current_age = age_4_reformation current_age = age_5_absolutism current_age = age_6_revolutions }
		}
	}
	effect = { set_variable = abm_focus_naval }
}

abm_focus_switch_naval = {
	scope = country
	is_shown = {
		NOT = { has_variable = abm_focus_naval }
		OR = { has_variable = abm_focus_quality has_variable = abm_focus_quantity }
	}
	is_valid = {
		custom_tooltip = {
			text = ABM_FOCUS_CHANGES_NONE_TT
			OR = {
				NOT = { has_variable = abm_focus_changes_used }
				var:abm_focus_changes_used < 3
			}
		}
		custom_tooltip = {
			text = ABM_FOCUS_NOTHING_RESEARCHED_TT
			NOT = { OR = { has_advance = formalized_officer_corps has_advance = administrative_leadership has_advance = quality_education has_advance = abm_drill_sergeants has_advance = abm_trained_march has_advance = military_drill has_advance = abm_rapid_reinforcement has_advance = camp_followers has_advance = the_old_and_infirm has_advance = abm_deep_reserves has_advance = supply_trains has_advance = grand_army } }
		}
	}
	effect = {
		if = { limit = { has_variable = abm_focus_quality } remove_variable = abm_focus_quality }
		if = { limit = { has_variable = abm_focus_quantity } remove_variable = abm_focus_quantity }
		set_variable = abm_focus_naval
		if = {
			limit = { has_variable = abm_focus_changes_used }
			change_variable = { name = abm_focus_changes_used add = 1 }
		}
		else = { set_variable = { name = abm_focus_changes_used value = 1 } }
	}
}

abm_focus_pick_religion = {
	scope = country
	is_shown = { NOT = { OR = { has_variable = abm_focus_religion has_variable = abm_focus_expansion has_variable = abm_focus_humanism } } }
	is_valid = {
		custom_tooltip = {
			text = abm_focus_panel.age_4_reformation.reached_tt
			OR = { current_age = age_4_reformation current_age = age_5_absolutism current_age = age_6_revolutions }
		}
	}
	effect = { set_variable = abm_focus_religion }
}

abm_focus_switch_religion = {
	scope = country
	is_shown = {
		NOT = { has_variable = abm_focus_religion }
		OR = { has_variable = abm_focus_expansion has_variable = abm_focus_humanism }
	}
	is_valid = {
		custom_tooltip = {
			text = ABM_FOCUS_CHANGES_NONE_TT
			OR = {
				NOT = { has_variable = abm_focus_changes_used }
				var:abm_focus_changes_used < 3
			}
		}
		custom_tooltip = {
			text = ABM_FOCUS_NOTHING_RESEARCHED_TT
			NOT = { OR = { has_advance = free_colonies has_advance = land_of_opportunity has_advance = additional_colonists has_advance = vice_roys has_advance = merchant_power_from_maritime_rev_dip_choice has_advance = overseas_merchants has_advance = ecumenism has_advance = humanist_tolerance has_advance = abm_integration_of_cultures has_advance = cultural_ties has_advance = abm_compromise has_advance = cultures_capacity_mil_choice } }
		}
	}
	effect = {
		if = { limit = { has_variable = abm_focus_expansion } remove_variable = abm_focus_expansion }
		if = { limit = { has_variable = abm_focus_humanism } remove_variable = abm_focus_humanism }
		set_variable = abm_focus_religion
		if = {
			limit = { has_variable = abm_focus_changes_used }
			change_variable = { name = abm_focus_changes_used add = 1 }
		}
		else = { set_variable = { name = abm_focus_changes_used value = 1 } }
	}
}

abm_focus_pick_expansion = {
	scope = country
	is_shown = { NOT = { OR = { has_variable = abm_focus_religion has_variable = abm_focus_expansion has_variable = abm_focus_humanism } } }
	is_valid = {
		custom_tooltip = {
			text = abm_focus_panel.age_4_reformation.reached_tt
			OR = { current_age = age_4_reformation current_age = age_5_absolutism current_age = age_6_revolutions }
		}
	}
	effect = { set_variable = abm_focus_expansion }
}

abm_focus_switch_expansion = {
	scope = country
	is_shown = {
		NOT = { has_variable = abm_focus_expansion }
		OR = { has_variable = abm_focus_religion has_variable = abm_focus_humanism }
	}
	is_valid = {
		custom_tooltip = {
			text = ABM_FOCUS_CHANGES_NONE_TT
			OR = {
				NOT = { has_variable = abm_focus_changes_used }
				var:abm_focus_changes_used < 3
			}
		}
		custom_tooltip = {
			text = ABM_FOCUS_NOTHING_RESEARCHED_TT
			NOT = { OR = { has_advance = devoutness has_advance = religious_tradition has_advance = abm_cultural_unity has_advance = preaching_missions has_advance = divine_supremacy has_advance = missionary_schools has_advance = ecumenism has_advance = humanist_tolerance has_advance = abm_integration_of_cultures has_advance = cultural_ties has_advance = abm_compromise has_advance = cultures_capacity_mil_choice } }
		}
	}
	effect = {
		if = { limit = { has_variable = abm_focus_religion } remove_variable = abm_focus_religion }
		if = { limit = { has_variable = abm_focus_humanism } remove_variable = abm_focus_humanism }
		set_variable = abm_focus_expansion
		if = {
			limit = { has_variable = abm_focus_changes_used }
			change_variable = { name = abm_focus_changes_used add = 1 }
		}
		else = { set_variable = { name = abm_focus_changes_used value = 1 } }
	}
}

abm_focus_pick_humanism = {
	scope = country
	is_shown = { NOT = { OR = { has_variable = abm_focus_religion has_variable = abm_focus_expansion has_variable = abm_focus_humanism } } }
	is_valid = {
		custom_tooltip = {
			text = abm_focus_panel.age_4_reformation.reached_tt
			OR = { current_age = age_4_reformation current_age = age_5_absolutism current_age = age_6_revolutions }
		}
	}
	effect = { set_variable = abm_focus_humanism }
}

abm_focus_switch_humanism = {
	scope = country
	is_shown = {
		NOT = { has_variable = abm_focus_humanism }
		OR = { has_variable = abm_focus_religion has_variable = abm_focus_expansion }
	}
	is_valid = {
		custom_tooltip = {
			text = ABM_FOCUS_CHANGES_NONE_TT
			OR = {
				NOT = { has_variable = abm_focus_changes_used }
				var:abm_focus_changes_used < 3
			}
		}
		custom_tooltip = {
			text = ABM_FOCUS_NOTHING_RESEARCHED_TT
			NOT = { OR = { has_advance = devoutness has_advance = religious_tradition has_advance = abm_cultural_unity has_advance = preaching_missions has_advance = divine_supremacy has_advance = missionary_schools has_advance = free_colonies has_advance = land_of_opportunity has_advance = additional_colonists has_advance = vice_roys has_advance = merchant_power_from_maritime_rev_dip_choice has_advance = overseas_merchants } }
		}
	}
	effect = {
		if = { limit = { has_variable = abm_focus_religion } remove_variable = abm_focus_religion }
		if = { limit = { has_variable = abm_focus_expansion } remove_variable = abm_focus_expansion }
		set_variable = abm_focus_humanism
		if = {
			limit = { has_variable = abm_focus_changes_used }
			change_variable = { name = abm_focus_changes_used add = 1 }
		}
		else = { set_variable = { name = abm_focus_changes_used value = 1 } }
	}
}

abm_focus_pick_offense = {
	scope = country
	is_shown = { NOT = { OR = { has_variable = abm_focus_offense has_variable = abm_focus_defense has_variable = abm_focus_littoral_fleet } } }
	is_valid = {
		custom_tooltip = {
			text = abm_focus_panel.age_4_reformation.reached_tt
			OR = { current_age = age_4_reformation current_age = age_5_absolutism current_age = age_6_revolutions }
		}
	}
	effect = { set_variable = abm_focus_offense }
}

abm_focus_switch_offense = {
	scope = country
	is_shown = {
		NOT = { has_variable = abm_focus_offense }
		OR = { has_variable = abm_focus_defense has_variable = abm_focus_littoral_fleet }
	}
	is_valid = {
		custom_tooltip = {
			text = ABM_FOCUS_CHANGES_NONE_TT
			OR = {
				NOT = { has_variable = abm_focus_changes_used }
				var:abm_focus_changes_used < 3
			}
		}
		custom_tooltip = {
			text = ABM_FOCUS_NOTHING_RESEARCHED_TT
			NOT = { OR = { has_advance = defensive_army has_advance = garrison_conscription has_advance = defensive_mentality has_advance = abm_earthworks has_advance = fort_logistics has_advance = abm_fortified_cities_focus has_advance = abm_gun_galleys has_advance = naval_cadets has_advance = merchant_marine has_advance = improved_shipyards has_advance = abm_home_fleet has_advance = sheltered_ports } }
		}
	}
	effect = {
		if = { limit = { has_variable = abm_focus_defense } remove_variable = abm_focus_defense }
		if = { limit = { has_variable = abm_focus_littoral_fleet } remove_variable = abm_focus_littoral_fleet }
		set_variable = abm_focus_offense
		if = {
			limit = { has_variable = abm_focus_changes_used }
			change_variable = { name = abm_focus_changes_used add = 1 }
		}
		else = { set_variable = { name = abm_focus_changes_used value = 1 } }
	}
}

abm_focus_pick_defense = {
	scope = country
	is_shown = { NOT = { OR = { has_variable = abm_focus_offense has_variable = abm_focus_defense has_variable = abm_focus_littoral_fleet } } }
	is_valid = {
		custom_tooltip = {
			text = abm_focus_panel.age_4_reformation.reached_tt
			OR = { current_age = age_4_reformation current_age = age_5_absolutism current_age = age_6_revolutions }
		}
	}
	effect = { set_variable = abm_focus_defense }
}

abm_focus_switch_defense = {
	scope = country
	is_shown = {
		NOT = { has_variable = abm_focus_defense }
		OR = { has_variable = abm_focus_offense has_variable = abm_focus_littoral_fleet }
	}
	is_valid = {
		custom_tooltip = {
			text = ABM_FOCUS_CHANGES_NONE_TT
			OR = {
				NOT = { has_variable = abm_focus_changes_used }
				var:abm_focus_changes_used < 3
			}
		}
		custom_tooltip = {
			text = ABM_FOCUS_NOTHING_RESEARCHED_TT
			NOT = { OR = { has_advance = expanded_supply_trains has_advance = abm_night_assaults has_advance = engineer_corps has_advance = abm_standardized_barrels has_advance = superior_firepower has_advance = abm_siege_artillery has_advance = abm_gun_galleys has_advance = naval_cadets has_advance = merchant_marine has_advance = improved_shipyards has_advance = abm_home_fleet has_advance = sheltered_ports } }
		}
	}
	effect = {
		if = { limit = { has_variable = abm_focus_offense } remove_variable = abm_focus_offense }
		if = { limit = { has_variable = abm_focus_littoral_fleet } remove_variable = abm_focus_littoral_fleet }
		set_variable = abm_focus_defense
		if = {
			limit = { has_variable = abm_focus_changes_used }
			change_variable = { name = abm_focus_changes_used add = 1 }
		}
		else = { set_variable = { name = abm_focus_changes_used value = 1 } }
	}
}

abm_focus_pick_littoral_fleet = {
	scope = country
	is_shown = { NOT = { OR = { has_variable = abm_focus_offense has_variable = abm_focus_defense has_variable = abm_focus_littoral_fleet } } }
	is_valid = {
		custom_tooltip = {
			text = abm_focus_panel.age_4_reformation.reached_tt
			OR = { current_age = age_4_reformation current_age = age_5_absolutism current_age = age_6_revolutions }
		}
	}
	effect = { set_variable = abm_focus_littoral_fleet }
}

abm_focus_switch_littoral_fleet = {
	scope = country
	is_shown = {
		NOT = { has_variable = abm_focus_littoral_fleet }
		OR = { has_variable = abm_focus_offense has_variable = abm_focus_defense }
	}
	is_valid = {
		custom_tooltip = {
			text = ABM_FOCUS_CHANGES_NONE_TT
			OR = {
				NOT = { has_variable = abm_focus_changes_used }
				var:abm_focus_changes_used < 3
			}
		}
		custom_tooltip = {
			text = ABM_FOCUS_NOTHING_RESEARCHED_TT
			NOT = { OR = { has_advance = expanded_supply_trains has_advance = abm_night_assaults has_advance = engineer_corps has_advance = abm_standardized_barrels has_advance = superior_firepower has_advance = abm_siege_artillery has_advance = defensive_army has_advance = garrison_conscription has_advance = defensive_mentality has_advance = abm_earthworks has_advance = fort_logistics has_advance = abm_fortified_cities_focus } }
		}
	}
	effect = {
		if = { limit = { has_variable = abm_focus_offense } remove_variable = abm_focus_offense }
		if = { limit = { has_variable = abm_focus_defense } remove_variable = abm_focus_defense }
		set_variable = abm_focus_littoral_fleet
		if = {
			limit = { has_variable = abm_focus_changes_used }
			change_variable = { name = abm_focus_changes_used add = 1 }
		}
		else = { set_variable = { name = abm_focus_changes_used value = 1 } }
	}
}

abm_focus_pick_administration = {
	scope = country
	is_shown = { NOT = { OR = { has_variable = abm_focus_administration has_variable = abm_focus_commerce has_variable = abm_focus_espionage } } }
	is_valid = {
		custom_tooltip = {
			text = abm_focus_panel.age_5_absolutism.reached_tt
			OR = { current_age = age_5_absolutism current_age = age_6_revolutions }
		}
	}
	effect = { set_variable = abm_focus_administration }
}

abm_focus_switch_administration = {
	scope = country
	is_shown = {
		NOT = { has_variable = abm_focus_administration }
		OR = { has_variable = abm_focus_commerce has_variable = abm_focus_espionage }
	}
	is_valid = {
		custom_tooltip = {
			text = ABM_FOCUS_CHANGES_NONE_TT
			OR = {
				NOT = { has_variable = abm_focus_changes_used }
				var:abm_focus_changes_used < 3
			}
		}
		custom_tooltip = {
			text = ABM_FOCUS_NOTHING_RESEARCHED_TT
			NOT = { OR = { has_advance = competetive_merchants has_advance = national_trade_policy has_advance = fast_negotiations has_advance = free_trade has_advance = trade_manipulation has_advance = bookkeeping has_advance = vetting has_advance = efficient_spies has_advance = infiltrate_administration_advance has_advance = foreign_embassies has_advance = abm_global_networks has_advance = abm_industrial_espionage } }
		}
	}
	effect = {
		if = { limit = { has_variable = abm_focus_commerce } remove_variable = abm_focus_commerce }
		if = { limit = { has_variable = abm_focus_espionage } remove_variable = abm_focus_espionage }
		set_variable = abm_focus_administration
		if = {
			limit = { has_variable = abm_focus_changes_used }
			change_variable = { name = abm_focus_changes_used add = 1 }
		}
		else = { set_variable = { name = abm_focus_changes_used value = 1 } }
	}
}

abm_focus_pick_commerce = {
	scope = country
	is_shown = { NOT = { OR = { has_variable = abm_focus_administration has_variable = abm_focus_commerce has_variable = abm_focus_espionage } } }
	is_valid = {
		custom_tooltip = {
			text = abm_focus_panel.age_5_absolutism.reached_tt
			OR = { current_age = age_5_absolutism current_age = age_6_revolutions }
		}
	}
	effect = { set_variable = abm_focus_commerce }
}

abm_focus_switch_commerce = {
	scope = country
	is_shown = {
		NOT = { has_variable = abm_focus_commerce }
		OR = { has_variable = abm_focus_administration has_variable = abm_focus_espionage }
	}
	is_valid = {
		custom_tooltip = {
			text = ABM_FOCUS_CHANGES_NONE_TT
			OR = {
				NOT = { has_variable = abm_focus_changes_used }
				var:abm_focus_changes_used < 3
			}
		}
		custom_tooltip = {
			text = ABM_FOCUS_NOTHING_RESEARCHED_TT
			NOT = { OR = { has_advance = cabinet has_advance = national_bank has_advance = civil_servants has_advance = legislative_efficiency_idea has_advance = centralized_government has_advance = economic_ideas has_advance = vetting has_advance = efficient_spies has_advance = infiltrate_administration_advance has_advance = foreign_embassies has_advance = abm_global_networks has_advance = abm_industrial_espionage } }
		}
	}
	effect = {
		if = { limit = { has_variable = abm_focus_administration } remove_variable = abm_focus_administration }
		if = { limit = { has_variable = abm_focus_espionage } remove_variable = abm_focus_espionage }
		set_variable = abm_focus_commerce
		if = {
			limit = { has_variable = abm_focus_changes_used }
			change_variable = { name = abm_focus_changes_used add = 1 }
		}
		else = { set_variable = { name = abm_focus_changes_used value = 1 } }
	}
}

abm_focus_pick_espionage = {
	scope = country
	is_shown = { NOT = { OR = { has_variable = abm_focus_administration has_variable = abm_focus_commerce has_variable = abm_focus_espionage } } }
	is_valid = {
		custom_tooltip = {
			text = abm_focus_panel.age_5_absolutism.reached_tt
			OR = { current_age = age_5_absolutism current_age = age_6_revolutions }
		}
	}
	effect = { set_variable = abm_focus_espionage }
}

abm_focus_switch_espionage = {
	scope = country
	is_shown = {
		NOT = { has_variable = abm_focus_espionage }
		OR = { has_variable = abm_focus_administration has_variable = abm_focus_commerce }
	}
	is_valid = {
		custom_tooltip = {
			text = ABM_FOCUS_CHANGES_NONE_TT
			OR = {
				NOT = { has_variable = abm_focus_changes_used }
				var:abm_focus_changes_used < 3
			}
		}
		custom_tooltip = {
			text = ABM_FOCUS_NOTHING_RESEARCHED_TT
			NOT = { OR = { has_advance = cabinet has_advance = national_bank has_advance = civil_servants has_advance = legislative_efficiency_idea has_advance = centralized_government has_advance = economic_ideas has_advance = competetive_merchants has_advance = national_trade_policy has_advance = fast_negotiations has_advance = free_trade has_advance = trade_manipulation has_advance = bookkeeping } }
		}
	}
	effect = {
		if = { limit = { has_variable = abm_focus_administration } remove_variable = abm_focus_administration }
		if = { limit = { has_variable = abm_focus_commerce } remove_variable = abm_focus_commerce }
		set_variable = abm_focus_espionage
		if = {
			limit = { has_variable = abm_focus_changes_used }
			change_variable = { name = abm_focus_changes_used add = 1 }
		}
		else = { set_variable = { name = abm_focus_changes_used value = 1 } }
	}
}

abm_focus_pick_maneuver = {
	scope = country
	is_shown = { NOT = { OR = { has_variable = abm_focus_maneuver has_variable = abm_focus_logistics has_variable = abm_focus_highseas_fleet } } }
	is_valid = {
		custom_tooltip = {
			text = abm_focus_panel.age_5_absolutism.reached_tt
			OR = { current_age = age_5_absolutism current_age = age_6_revolutions }
		}
	}
	effect = { set_variable = abm_focus_maneuver }
}

abm_focus_switch_maneuver = {
	scope = country
	is_shown = {
		NOT = { has_variable = abm_focus_maneuver }
		OR = { has_variable = abm_focus_logistics has_variable = abm_focus_highseas_fleet }
	}
	is_valid = {
		custom_tooltip = {
			text = ABM_FOCUS_CHANGES_NONE_TT
			OR = {
				NOT = { has_variable = abm_focus_changes_used }
				var:abm_focus_changes_used < 3
			}
		}
		custom_tooltip = {
			text = ABM_FOCUS_NOTHING_RESEARCHED_TT
			NOT = { OR = { has_advance = abm_enduring_hardships has_advance = improved_foraging has_advance = abm_prepared_camps has_advance = improved_logistics has_advance = abm_logistics_corps has_advance = abm_trained_reserves has_advance = abm_ships_of_the_line has_advance = sea_hawks has_advance = escort_ships has_advance = superior_seamanship has_advance = naval_fighting_instruction has_advance = press_gangs } }
		}
	}
	effect = {
		if = { limit = { has_variable = abm_focus_logistics } remove_variable = abm_focus_logistics }
		if = { limit = { has_variable = abm_focus_highseas_fleet } remove_variable = abm_focus_highseas_fleet }
		set_variable = abm_focus_maneuver
		if = {
			limit = { has_variable = abm_focus_changes_used }
			change_variable = { name = abm_focus_changes_used add = 1 }
		}
		else = { set_variable = { name = abm_focus_changes_used value = 1 } }
	}
}

abm_focus_pick_logistics = {
	scope = country
	is_shown = { NOT = { OR = { has_variable = abm_focus_maneuver has_variable = abm_focus_logistics has_variable = abm_focus_highseas_fleet } } }
	is_valid = {
		custom_tooltip = {
			text = abm_focus_panel.age_5_absolutism.reached_tt
			OR = { current_age = age_5_absolutism current_age = age_6_revolutions }
		}
	}
	effect = { set_variable = abm_focus_logistics }
}

abm_focus_switch_logistics = {
	scope = country
	is_shown = {
		NOT = { has_variable = abm_focus_logistics }
		OR = { has_variable = abm_focus_maneuver has_variable = abm_focus_highseas_fleet }
	}
	is_valid = {
		custom_tooltip = {
			text = ABM_FOCUS_CHANGES_NONE_TT
			OR = {
				NOT = { has_variable = abm_focus_changes_used }
				var:abm_focus_changes_used < 3
			}
		}
		custom_tooltip = {
			text = ABM_FOCUS_NOTHING_RESEARCHED_TT
			NOT = { OR = { has_advance = improved_manuever has_advance = abm_forced_march has_advance = land_morale_movement_cost_rev_choice_mil has_advance = abm_bold_maneuvers has_advance = abm_limbers has_advance = offensive_army has_advance = abm_ships_of_the_line has_advance = sea_hawks has_advance = escort_ships has_advance = superior_seamanship has_advance = naval_fighting_instruction has_advance = press_gangs } }
		}
	}
	effect = {
		if = { limit = { has_variable = abm_focus_maneuver } remove_variable = abm_focus_maneuver }
		if = { limit = { has_variable = abm_focus_highseas_fleet } remove_variable = abm_focus_highseas_fleet }
		set_variable = abm_focus_logistics
		if = {
			limit = { has_variable = abm_focus_changes_used }
			change_variable = { name = abm_focus_changes_used add = 1 }
		}
		else = { set_variable = { name = abm_focus_changes_used value = 1 } }
	}
}

abm_focus_pick_highseas_fleet = {
	scope = country
	is_shown = { NOT = { OR = { has_variable = abm_focus_maneuver has_variable = abm_focus_logistics has_variable = abm_focus_highseas_fleet } } }
	is_valid = {
		custom_tooltip = {
			text = abm_focus_panel.age_5_absolutism.reached_tt
			OR = { current_age = age_5_absolutism current_age = age_6_revolutions }
		}
	}
	effect = { set_variable = abm_focus_highseas_fleet }
}

abm_focus_switch_highseas_fleet = {
	scope = country
	is_shown = {
		NOT = { has_variable = abm_focus_highseas_fleet }
		OR = { has_variable = abm_focus_maneuver has_variable = abm_focus_logistics }
	}
	is_valid = {
		custom_tooltip = {
			text = ABM_FOCUS_CHANGES_NONE_TT
			OR = {
				NOT = { has_variable = abm_focus_changes_used }
				var:abm_focus_changes_used < 3
			}
		}
		custom_tooltip = {
			text = ABM_FOCUS_NOTHING_RESEARCHED_TT
			NOT = { OR = { has_advance = improved_manuever has_advance = abm_forced_march has_advance = land_morale_movement_cost_rev_choice_mil has_advance = abm_bold_maneuvers has_advance = abm_limbers has_advance = offensive_army has_advance = abm_enduring_hardships has_advance = improved_foraging has_advance = abm_prepared_camps has_advance = improved_logistics has_advance = abm_logistics_corps has_advance = abm_trained_reserves } }
		}
	}
	effect = {
		if = { limit = { has_variable = abm_focus_maneuver } remove_variable = abm_focus_maneuver }
		if = { limit = { has_variable = abm_focus_logistics } remove_variable = abm_focus_logistics }
		set_variable = abm_focus_highseas_fleet
		if = {
			limit = { has_variable = abm_focus_changes_used }
			change_variable = { name = abm_focus_changes_used add = 1 }
		}
		else = { set_variable = { name = abm_focus_changes_used value = 1 } }
	}
}

abm_focus_pick_industry = {
	scope = country
	is_shown = { NOT = { OR = { has_variable = abm_focus_industry has_variable = abm_focus_empire has_variable = abm_focus_innovation } } }
	is_valid = {
		custom_tooltip = {
			text = abm_focus_panel.age_6_revolutions.reached_tt
			OR = { current_age = age_6_revolutions }
		}
	}
	effect = { set_variable = abm_focus_industry }
}

abm_focus_switch_industry = {
	scope = country
	is_shown = {
		NOT = { has_variable = abm_focus_industry }
		OR = { has_variable = abm_focus_empire has_variable = abm_focus_innovation }
	}
	is_valid = {
		custom_tooltip = {
			text = ABM_FOCUS_CHANGES_NONE_TT
			OR = {
				NOT = { has_variable = abm_focus_changes_used }
				var:abm_focus_changes_used < 3
			}
		}
		custom_tooltip = {
			text = ABM_FOCUS_NOTHING_RESEARCHED_TT
			NOT = { OR = { has_advance = global_empire has_advance = local_traditions has_advance = postal_service has_advance = port_cost_distance_from_capital_choice_mil has_advance = abm_imperial_administration has_advance = abm_overseas_holdings has_advance = abm_national_academy_of_sciences has_advance = abm_the_scientific_method has_advance = abm_belle_epoque has_advance = humanism has_advance = abm_universal_literacy has_advance = optimism } }
		}
	}
	effect = {
		if = { limit = { has_variable = abm_focus_empire } remove_variable = abm_focus_empire }
		if = { limit = { has_variable = abm_focus_innovation } remove_variable = abm_focus_innovation }
		set_variable = abm_focus_industry
		if = {
			limit = { has_variable = abm_focus_changes_used }
			change_variable = { name = abm_focus_changes_used add = 1 }
		}
		else = { set_variable = { name = abm_focus_changes_used value = 1 } }
	}
}

abm_focus_pick_empire = {
	scope = country
	is_shown = { NOT = { OR = { has_variable = abm_focus_industry has_variable = abm_focus_empire has_variable = abm_focus_innovation } } }
	is_valid = {
		custom_tooltip = {
			text = abm_focus_panel.age_6_revolutions.reached_tt
			OR = { current_age = age_6_revolutions }
		}
	}
	effect = { set_variable = abm_focus_empire }
}

abm_focus_switch_empire = {
	scope = country
	is_shown = {
		NOT = { has_variable = abm_focus_empire }
		OR = { has_variable = abm_focus_industry has_variable = abm_focus_innovation }
	}
	is_valid = {
		custom_tooltip = {
			text = ABM_FOCUS_CHANGES_NONE_TT
			OR = {
				NOT = { has_variable = abm_focus_changes_used }
				var:abm_focus_changes_used < 3
			}
		}
		custom_tooltip = {
			text = ABM_FOCUS_NOTHING_RESEARCHED_TT
			NOT = { OR = { has_advance = audit_checks has_advance = formal_administration has_advance = smithian_economics has_advance = regulation_bureau has_advance = abm_industrial_scale_extraction has_advance = abm_modern_extraction_methods has_advance = abm_national_academy_of_sciences has_advance = abm_the_scientific_method has_advance = abm_belle_epoque has_advance = humanism has_advance = abm_universal_literacy has_advance = optimism } }
		}
	}
	effect = {
		if = { limit = { has_variable = abm_focus_industry } remove_variable = abm_focus_industry }
		if = { limit = { has_variable = abm_focus_innovation } remove_variable = abm_focus_innovation }
		set_variable = abm_focus_empire
		if = {
			limit = { has_variable = abm_focus_changes_used }
			change_variable = { name = abm_focus_changes_used add = 1 }
		}
		else = { set_variable = { name = abm_focus_changes_used value = 1 } }
	}
}

abm_focus_pick_innovation = {
	scope = country
	is_shown = { NOT = { OR = { has_variable = abm_focus_industry has_variable = abm_focus_empire has_variable = abm_focus_innovation } } }
	is_valid = {
		custom_tooltip = {
			text = abm_focus_panel.age_6_revolutions.reached_tt
			OR = { current_age = age_6_revolutions }
		}
	}
	effect = { set_variable = abm_focus_innovation }
}

abm_focus_switch_innovation = {
	scope = country
	is_shown = {
		NOT = { has_variable = abm_focus_innovation }
		OR = { has_variable = abm_focus_industry has_variable = abm_focus_empire }
	}
	is_valid = {
		custom_tooltip = {
			text = ABM_FOCUS_CHANGES_NONE_TT
			OR = {
				NOT = { has_variable = abm_focus_changes_used }
				var:abm_focus_changes_used < 3
			}
		}
		custom_tooltip = {
			text = ABM_FOCUS_NOTHING_RESEARCHED_TT
			NOT = { OR = { has_advance = audit_checks has_advance = formal_administration has_advance = smithian_economics has_advance = regulation_bureau has_advance = abm_industrial_scale_extraction has_advance = abm_modern_extraction_methods has_advance = global_empire has_advance = local_traditions has_advance = postal_service has_advance = port_cost_distance_from_capital_choice_mil has_advance = abm_imperial_administration has_advance = abm_overseas_holdings } }
		}
	}
	effect = {
		if = { limit = { has_variable = abm_focus_industry } remove_variable = abm_focus_industry }
		if = { limit = { has_variable = abm_focus_empire } remove_variable = abm_focus_empire }
		set_variable = abm_focus_innovation
		if = {
			limit = { has_variable = abm_focus_changes_used }
			change_variable = { name = abm_focus_changes_used add = 1 }
		}
		else = { set_variable = { name = abm_focus_changes_used value = 1 } }
	}
}

abm_focus_pick_voluntary_service = {
	scope = country
	is_shown = { NOT = { OR = { has_variable = abm_focus_voluntary_service has_variable = abm_focus_conscription has_variable = abm_focus_insurrection } } }
	is_valid = {
		custom_tooltip = {
			text = abm_focus_panel.age_6_revolutions.reached_tt
			OR = { current_age = age_6_revolutions }
		}
	}
	effect = { set_variable = abm_focus_voluntary_service }
}

abm_focus_switch_voluntary_service = {
	scope = country
	is_shown = {
		NOT = { has_variable = abm_focus_voluntary_service }
		OR = { has_variable = abm_focus_conscription has_variable = abm_focus_insurrection }
	}
	is_valid = {
		custom_tooltip = {
			text = ABM_FOCUS_CHANGES_NONE_TT
			OR = {
				NOT = { has_variable = abm_focus_changes_used }
				var:abm_focus_changes_used < 3
			}
		}
		custom_tooltip = {
			text = ABM_FOCUS_NOTHING_RESEARCHED_TT
			NOT = { OR = { has_advance = mass_army has_advance = national_conscripts has_advance = nationalistic_enthusiasm has_advance = the_young_can_serve has_advance = massed_battery has_advance = private_to_marshal has_advance = abm_guerillas has_advance = morale_recovery_rev_dip_choice has_advance = abm_peoples_war has_advance = abm_last_stand has_advance = abm_national_resistance has_advance = abm_foreign_fighters } }
		}
	}
	effect = {
		if = { limit = { has_variable = abm_focus_conscription } remove_variable = abm_focus_conscription }
		if = { limit = { has_variable = abm_focus_insurrection } remove_variable = abm_focus_insurrection }
		set_variable = abm_focus_voluntary_service
		if = {
			limit = { has_variable = abm_focus_changes_used }
			change_variable = { name = abm_focus_changes_used add = 1 }
		}
		else = { set_variable = { name = abm_focus_changes_used value = 1 } }
	}
}

abm_focus_pick_conscription = {
	scope = country
	is_shown = { NOT = { OR = { has_variable = abm_focus_voluntary_service has_variable = abm_focus_conscription has_variable = abm_focus_insurrection } } }
	is_valid = {
		custom_tooltip = {
			text = abm_focus_panel.age_6_revolutions.reached_tt
			OR = { current_age = age_6_revolutions }
		}
	}
	effect = { set_variable = abm_focus_conscription }
}

abm_focus_switch_conscription = {
	scope = country
	is_shown = {
		NOT = { has_variable = abm_focus_conscription }
		OR = { has_variable = abm_focus_voluntary_service has_variable = abm_focus_insurrection }
	}
	is_valid = {
		custom_tooltip = {
			text = ABM_FOCUS_CHANGES_NONE_TT
			OR = {
				NOT = { has_variable = abm_focus_changes_used }
				var:abm_focus_changes_used < 3
			}
		}
		custom_tooltip = {
			text = ABM_FOCUS_NOTHING_RESEARCHED_TT
			NOT = { OR = { has_advance = discipline_drills has_advance = abm_thorough_training has_advance = regimental_system has_advance = abm_skirmish_line has_advance = finest_of_horses has_advance = bayonet_leaders has_advance = abm_guerillas has_advance = morale_recovery_rev_dip_choice has_advance = abm_peoples_war has_advance = abm_last_stand has_advance = abm_national_resistance has_advance = abm_foreign_fighters } }
		}
	}
	effect = {
		if = { limit = { has_variable = abm_focus_voluntary_service } remove_variable = abm_focus_voluntary_service }
		if = { limit = { has_variable = abm_focus_insurrection } remove_variable = abm_focus_insurrection }
		set_variable = abm_focus_conscription
		if = {
			limit = { has_variable = abm_focus_changes_used }
			change_variable = { name = abm_focus_changes_used add = 1 }
		}
		else = { set_variable = { name = abm_focus_changes_used value = 1 } }
	}
}

abm_focus_pick_insurrection = {
	scope = country
	is_shown = { NOT = { OR = { has_variable = abm_focus_voluntary_service has_variable = abm_focus_conscription has_variable = abm_focus_insurrection } } }
	is_valid = {
		custom_tooltip = {
			text = abm_focus_panel.age_6_revolutions.reached_tt
			OR = { current_age = age_6_revolutions }
		}
	}
	effect = { set_variable = abm_focus_insurrection }
}

abm_focus_switch_insurrection = {
	scope = country
	is_shown = {
		NOT = { has_variable = abm_focus_insurrection }
		OR = { has_variable = abm_focus_voluntary_service has_variable = abm_focus_conscription }
	}
	is_valid = {
		custom_tooltip = {
			text = ABM_FOCUS_CHANGES_NONE_TT
			OR = {
				NOT = { has_variable = abm_focus_changes_used }
				var:abm_focus_changes_used < 3
			}
		}
		custom_tooltip = {
			text = ABM_FOCUS_NOTHING_RESEARCHED_TT
			NOT = { OR = { has_advance = discipline_drills has_advance = abm_thorough_training has_advance = regimental_system has_advance = abm_skirmish_line has_advance = finest_of_horses has_advance = bayonet_leaders has_advance = mass_army has_advance = national_conscripts has_advance = nationalistic_enthusiasm has_advance = the_young_can_serve has_advance = massed_battery has_advance = private_to_marshal } }
		}
	}
	effect = {
		if = { limit = { has_variable = abm_focus_voluntary_service } remove_variable = abm_focus_voluntary_service }
		if = { limit = { has_variable = abm_focus_conscription } remove_variable = abm_focus_conscription }
		set_variable = abm_focus_insurrection
		if = {
			limit = { has_variable = abm_focus_changes_used }
			change_variable = { name = abm_focus_changes_used add = 1 }
		}
		else = { set_variable = { name = abm_focus_changes_used value = 1 } }
	}
}

abm_focus_pending = {
	scope = country
	is_shown = {
		OR = {
			AND = {
				current_age = age_2_renaissance
				OR = {
					NOT = { OR = { has_variable = abm_focus_court has_variable = abm_focus_trade has_variable = abm_focus_influence } }
					NOT = { OR = { has_variable = abm_focus_levy has_variable = abm_focus_mercenary has_variable = abm_focus_maritime } }
				}
			}
			AND = {
				current_age = age_3_discovery
				OR = {
					NOT = { OR = { has_variable = abm_focus_infrastructure has_variable = abm_focus_exploration has_variable = abm_focus_diplomatic } }
					NOT = { OR = { has_variable = abm_focus_quality has_variable = abm_focus_quantity has_variable = abm_focus_naval } }
				}
			}
			AND = {
				current_age = age_4_reformation
				OR = {
					NOT = { OR = { has_variable = abm_focus_religion has_variable = abm_focus_expansion has_variable = abm_focus_humanism } }
					NOT = { OR = { has_variable = abm_focus_offense has_variable = abm_focus_defense has_variable = abm_focus_littoral_fleet } }
				}
			}
			AND = {
				current_age = age_5_absolutism
				OR = {
					NOT = { OR = { has_variable = abm_focus_administration has_variable = abm_focus_commerce has_variable = abm_focus_espionage } }
					NOT = { OR = { has_variable = abm_focus_maneuver has_variable = abm_focus_logistics has_variable = abm_focus_highseas_fleet } }
				}
			}
			AND = {
				current_age = age_6_revolutions
				OR = {
					NOT = { OR = { has_variable = abm_focus_industry has_variable = abm_focus_empire has_variable = abm_focus_innovation } }
					NOT = { OR = { has_variable = abm_focus_voluntary_service has_variable = abm_focus_conscription has_variable = abm_focus_insurrection } }
				}
			}
		}
	}
}
