# Per-country toggle for the "over alliance limit" popup warning. Operates on
# the calling country (root). Default state (variable absent) is muted.
mp_limits_toggle_alliance_alert = {
	effect = {
		root = {
			if = {
				limit = { has_variable = mp_alliance_alert_enabled }
				remove_variable = mp_alliance_alert_enabled
			}
			else = {
				set_variable = { name = mp_alliance_alert_enabled value = 1 }
			}
		}
	}
}

# Per-country toggle for the "over fort limit" popup warning.
# Default state: ON (variable absent). Setting mp_fort_alert_muted mutes it.
mp_limits_toggle_fort_alert = {
	effect = {
		root = {
			if = {
				limit = { has_variable = mp_fort_alert_muted }
				remove_variable = mp_fort_alert_muted
			}
			else = {
				set_variable = { name = mp_fort_alert_muted value = 1 }
			}
		}
	}
}

# Per-country toggle for the "over Urban Capacity" popup warning (abm_urbanisation.1).
# Default state: ON (variable absent). Setting abm_urban_alert_muted mutes it.
abm_toggle_urban_alert = {
	effect = {
		root = {
			if = {
				limit = { has_variable = abm_urban_alert_muted }
				remove_variable = abm_urban_alert_muted
			}
			else = {
				set_variable = { name = abm_urban_alert_muted value = 1 }
			}
		}
	}
}
