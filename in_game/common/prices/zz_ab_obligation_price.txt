# TEMPORARY — CAPITALISM PHASE 1 SPIKE. DELETE AFTER VALIDATION.
# Base price for the obligation IO payment; the real per-month sum comes from
# price_multiplier = var:ab_monthly on the IO instance, so base is just 1 gold.
ab_obligation_price = {
	gold = 1
}
