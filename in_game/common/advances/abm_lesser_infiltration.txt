# Ars Belli: general-tree replacement for infiltrate_administration_advance, which moved into the
# Espionage focus pick (abm_focus_picks_economic.txt). Same slot as vanilla (after spy_construction_renaissance).
abm_lesser_infiltration_advance = {
	age = age_2_renaissance
	icon = efficient_spies

	unlock_relation_type = abm_lesser_infiltration

	requires = spy_construction_renaissance
}
