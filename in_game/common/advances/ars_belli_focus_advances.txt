# Advances gated by the second-set age focuses chosen in ars_belli_age_focus.1/.2.
# Player picks 2 of 3 focuses, each setting a country flag:
#   ars_belli_focus_expansion / ars_belli_focus_commerce / ars_belli_focus_warfare
#
# Pattern:
#   - No "for = adm|dip|mil" (that field hooks the engine's preference slot;
#     we use a script-side gate instead).
#   - allow = { has_country_flag = ars_belli_focus_<focus> } blocks the advance
#     until the matching flag is set. An advance unlocks if EITHER of the two
#     picks matches its flag, which is the intended 2-of-3 behavior.
#   - Replicate per age by changing "age = age_X_*" and the modifiers below.
############################
# age_2_renaissance — Expansion / Commerce / Warfare focus picks
############################

ars_belli_expansion_settlement_drive = {
	age = age_2_renaissance
	icon = scholasticism

	allow = {
		has_country_flag = ars_belli_focus_expansion
	}

	# Placeholder modifier — replace with the intended payoff.
	global_settler_growth = 5
}

ars_belli_commerce_merchant_charters = {
	age = age_2_renaissance
	icon = court_accounting

	allow = {
		has_country_flag = ars_belli_focus_commerce
	}

	# Placeholder modifier — replace with the intended payoff.
	global_trade_goods_size_modifier = 0.05
}

ars_belli_warfare_drilled_levies = {
	age = age_2_renaissance
	icon = regimental_system

	allow = {
		has_country_flag = ars_belli_focus_warfare
	}

	# Placeholder modifier — replace with the intended payoff.
	land_morale = 0.05
}
