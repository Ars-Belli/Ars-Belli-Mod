# Ars Belli: same-name replacement that empties vanilla 4_choices_mil.txt.
# The engine adm/dip/mil age focus (`for = mil`) is replaced by the Ars Belli focus picks:
# two picks per age (Economic + Military), each out of 3 branches. The advances that were
# kept live in abm_focus_picks_economic.txt / abm_focus_picks_military.txt under their vanilla keys.
