# Logistic (auxiliary) unit food_storage_per_strength buff.
# Vanilla base inherited from army_auxiliary category: 2000 (1x).
# Scales from 2x (Traditions) up to 5x (Revolutions) on a non-linear curve
# so later-age logistic units carry a much larger food buffer.

REPLACE:a_camp_followers = {
	copy_from = a_age_1_traditions_auxiliary
	gfx_tags = { medium_tag }

	country_potential = {
		and = {
			exists = government_type
			government_type != government_type:steppe_horde
		}
	}

	mercenaries_per_location = {
		pop_type = clergy
		multiply = 0.01
	}

	upgrades_to = a_supply_carts

	food_storage_per_strength = 4000	# was 2000 (1x), now 2x
}

REPLACE:a_supply_carts = {
	copy_from = a_age_2_renaissance_auxiliary

	mercenaries_per_location = {
		pop_type = clergy
		multiply = 0.01
	}
	gfx_tags = { medium_tag }

	upgrades_to = a_supply_convoy

	food_storage_per_strength = 5000	# was 2000 (1x), now 2.5x
}

REPLACE:a_supply_convoy = {
	copy_from = a_age_3_discovery_auxiliary

	mercenaries_per_location = {
		pop_type = clergy
		multiply = 0.01
	}

	upgrades_to = a_baggage_train

	food_storage_per_strength = 6000	# was 2000 (1x), now 3x
}

REPLACE:a_baggage_train = {
	copy_from = a_age_4_reformation_auxiliary

	mercenaries_per_location = {
		pop_type = clergy
		multiply = 0.01
	}
	gfx_tags = { medium_tag }

	upgrades_to = a_wagon_train

	food_storage_per_strength = 7000	# was 2000 (1x), now 3.5x
}

REPLACE:a_wagon_train = {
	copy_from = a_age_5_absolutism_auxiliary

	mercenaries_per_location = {
		pop_type = clergy
		multiply = 0.01
	}
	gfx_tags = { medium_tag }

	upgrades_to = a_logistics_corps

	food_storage_per_strength = 9000	# was 2000 (1x), now 4.5x
}

REPLACE:a_logistics_corps = {
	copy_from = a_age_6_revolutions_auxiliary

	mercenaries_per_location = {
		pop_type = clergy
		multiply = 0.01
	}
	gfx_tags = { medium_tag }

	food_storage_per_strength = 10000	# was 2000 (1x), now 5x
}
