types = {
    type[holy_site] = {
        path = "common/holy_sites"
        path = "in_game/common/holy_sites"
        path = "main_menu/common/holy_sites"
        path = "loading_screen/common/holy_sites"
        path_extension = .txt
        localisation = {
            name = "$"
        }
    }
}

holy_site = {
    location = value[location]

    type = <holy_site_type>

    importance = int

    religions = {
        ## cardinality = 1..inf
        <religion>
    }

    ## cardinality = 0..1
    god = <god>

    ## cardinality = 0..1
    avatar = <avatar>
}
