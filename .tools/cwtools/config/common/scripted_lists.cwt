types = {
    type[scripted_list] = {
        path = "common/scripted_lists"
        path = "in_game/common/scripted_lists"
        path = "main_menu/common/scripted_lists"
        path = "loading_screen/common/scripted_lists"
        path_extension = .txt
    }
}

scripted_list = {
    base = enum[scripted_list_base]

    conditions = single_alias_right[trigger_clause]
}
